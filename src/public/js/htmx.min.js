/*!
 * htmx — vendored placeholder
 * Pinned version: 1.9.12
 * Upstream: https://unpkg.com/htmx.org@1.9.12/dist/htmx.min.js
 * License: BSD-2-Clause (see https://github.com/bigskysoftware/htmx/blob/master/LICENSE)
 *
 * Expected SRI integrity (SHA-384, base64):
 *   sha384-ujb1lZYygJmzgSwoxRggbCHcjc0rB2XoQrxeTUQyRjrOnlCoYta87iKBWq3EsdM2
 *
 * The hash above is computed from the upstream payload at the pinned version
 * (verified locally on 2024-12; recompute via `npm run vendor:js`). Once this
 * file is replaced with the real minified payload, the script also writes
 * src/public/js/sri-manifest.json so views/partials/header.njk can reference
 * `integrity="sha384-..." crossorigin="anonymous"` consistently.
 *
 * TODO_FILL_AFTER_DOWNLOAD — replace this placeholder during deploy bootstrap.
 *   Either run `npm run vendor:js` after `npm install` (postinstall step) or
 *   manually download from the upstream URL above. CI may fail-fast with
 *   `npm run vendor:js:check` to catch a forgotten step.
 *
 * Validates: Requirements 2.10 (no external CDN dependency), 15.1 (CSP-friendly,
 * SRI integrity for vendored assets).
 */
throw new Error(
  '[ptk-app] htmx.min.js is a vendor placeholder. Run `npm run vendor:js` to populate it.'
);
