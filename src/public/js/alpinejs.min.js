/*!
 * Alpine.js — vendored placeholder
 * Pinned version: 3.13.10
 * Upstream: https://unpkg.com/alpinejs@3.13.10/dist/cdn.min.js
 * License: MIT (see https://github.com/alpinejs/alpine/blob/main/LICENSE.md)
 *
 * Expected SRI integrity (SHA-384, base64):
 *   sha384-XBJ5+bq4ga1+0s+J4sl6njqQ9C/YIfKeQw18HypSuGEaPm1g/VWaNdsQ5d3sE1qi
 *
 * Loading note: Alpine.js must be loaded with the `defer` attribute on the
 * <script> tag so component initialization waits for DOMContentLoaded. See
 * src/views/partials/header.njk (added in a later task).
 *
 * TODO_FILL_AFTER_DOWNLOAD — replace this placeholder during deploy bootstrap.
 *   Either run `npm run vendor:js` after `npm install` (postinstall step) or
 *   manually download from the upstream URL above. CI may fail-fast with
 *   `npm run vendor:js:check` to catch a forgotten step.
 *
 * Validates: Requirements 2.10 (no external CDN dependency), 15.1 (CSP-friendly,
 * SRI integrity for vendored assets).
 */
throw new Error(
  '[ptk-app] alpinejs.min.js is a vendor placeholder. Run `npm run vendor:js` to populate it.'
);
