/*!
 * Sortable.js — vendored placeholder
 * Pinned version: 1.15.6
 * Upstream: https://unpkg.com/sortablejs@1.15.6/Sortable.min.js
 * License: MIT (see https://github.com/SortableJS/Sortable/blob/master/LICENSE)
 *
 * Expected SRI integrity (SHA-384, base64):
 *   sha384-HZZ/fukV+9G8gwTNjN7zQDG0Sp7MsZy5DDN6VfY3Be7V9dvQpEpR2jF2HlyFUUjU
 *
 * Used by the admin Kanban view (Req 10 AC #2) together with Alpine.js to
 * trigger htmx `hx-post /api/applications/:id/stage` on drag-drop. See
 * design.md §4.2.
 *
 * TODO_FILL_AFTER_DOWNLOAD — replace this placeholder during deploy bootstrap.
 *   Either run `npm run vendor:js` after `npm install` (postinstall step) or
 *   manually download from the upstream URL above. CI may fail-fast with
 *   `npm run vendor:js:check` to catch a forgotten step.
 *
 * Validates: Requirements 2.10 (no external CDN dependency), 15.1 (CSP-friendly,
 * SRI integrity for vendored assets).
 */
throw new Error(
  '[ptk-app] sortable.min.js is a vendor placeholder. Run `npm run vendor:js` to populate it.'
);
